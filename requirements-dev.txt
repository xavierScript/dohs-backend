# Evaluation-only dependencies for DOHS academic research evaluation
# Install with: pip install -r requirements-dev.txt
# DO NOT add these to requirements.txt — they are not needed in production

# RQ1: DBSCAN benchmarking
scikit-learn>=1.3.0

# RQ2: Load testing
locust>=2.20.0

# RQ2: Latency profiling (already in production, listed here for explicit dev install)
httpx>=0.25.0
